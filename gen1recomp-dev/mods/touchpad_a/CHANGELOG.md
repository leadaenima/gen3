# Changelog

Format: [keep a changelog](https://keepachangelog.com/en/1.1.0/).
Version headings match `manifest.json`'s `version`.

## 1.1.0

### Changed

- A held finger re-taps A a few times per second instead of holding A
  down, so menus and text keep getting `wasPressed` edges.

### Added

- `HOLD TAPS / SEC` option (2–10, default 5).

## 1.0.0

### Added

- Uncaptured screen touches press Game Boy A.
- `ENABLED` and `MOUSE CLICKS` mod options.
